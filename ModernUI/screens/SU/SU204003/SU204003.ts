import { PXScreen, graphInfo } from "client-controls";

@graphInfo({
    graphType: "PX.Survey.Ext.SurveyComponentMaint",
    primaryView: "SUComponent",
})
export class SU204003 extends PXScreen {
}
